import setuptools

setuptools.setup (
    name = 'ekfparse',
    version='0.0.1',
    author='Erin Kado-Fong',
    description='handy dandy parsing utils',
    packages=setuptools.find_packages(),
    python_requires='>=3.7'
    )
